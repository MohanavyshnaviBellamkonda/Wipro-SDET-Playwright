DOM manipulatio 

string literals - it is the feature of ES6
                - it can be represent by using back ticks(` `)
                - We need to add dollar sign and curly braces for literal 

                Example: let name = "vyshu"
                         console.log('My name is ${name}) // My name is vyshu
                    
                we can also done it by using inverted commas, but it looks clumsy for long code so we use this instead

//Example
function table(num) {
     let result = "";
     for(let i=1; i<=10; i++) {
          result += `${num} x ${i} = ${num *i}\n`;
     }
     return result;
}

//Arow function

Arrow function is the best way of defining function, used to minimize the code into a single line

//let sum1 = sun(5,10);
let sum = (x, y) => x+y;
console.log("Sum of 5 and 10:", sum(5,10));

let arr = [1, 2, 3, 4, 5, "Aaryan"];

// arr.filter(function(x) {
//     return typeof x === "number";
// }).map(function(y) {
//     return y * 2;
// }).forEach(function(abc) {
//     console.log("Element after * 2:", abc * 2);
// });

arr.filter(x => typeof x === "number").map(y => y *2).forEach(abc => console.log("Element after * 2:", abc * 2))

- filter() - used to seperate the same type of data from the array 
-map() - used to transform every element in an array and create a new array with the results
-random()- used to get the random value between 0 and 1
-floor() - function is used to get the lower bound value of a float value 
        ex: math.floor(0.99999) is 0


//console.log(Math.floor(Math.random() * 100) + 1); // 1 to 100
// floor -> rounds down to the nearest integer 0.9 --> 0
// 0 --> 99.999 -> 99 [0 - 99]

// Math.random --> 0 - 1 * 10 --> 0 - 10
// Math.floor(x) --> 0.3 (0, 1) = 0, 1.6 (1, 2) = 1, 3.998 (3, 4) = 3
// floor(0.1, 9.999) -> [0, 9]

Typecasting:

-string to number
-Number to string

let strnum = "5";
console.log(Number(strnum));   //convert the string into number
console.log(String(5)); // convert the number into string

then we learnt typecating into integer and float

let val = "0.6";
console.log(parseInt(val)); //0 it will convert string into integer
console.log(parseFloat(val)); // 0.6 it will convert string into float

// floor [int] -> Low int, parseInt -> type conversion (string -> int)


//Example
let colors = ["red", "green" , "blue"];
console.log(colors.indexOf("yellow"));
console.log(colors.includes("yellow"));
console.log(colors.length);
console.log(colors[3]); // undefined
console.log(colors[colors.length - 1]);
colors.push("yellow"); 
console.log(colors);
colors.pop();
console.log(colors);
colors.unshift("purple"); 
console.log(colors); 
colors.shift(); 
console.log(colors);

splice(): this function is used to mutates the original array


console.log(colors.splice(0, 1));
console.log(colors.splice(1, 2));
console.log(colors.splice(1, 3));

And then we built some html,css files 


